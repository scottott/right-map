[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# Sync functions default to ~10s on Netlify; osrm-proxy stays under that (parallel 6.5s upstream).
# Pro: you can raise timeout in Site settings → Functions if needed.
